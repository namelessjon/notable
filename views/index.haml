!!!
%html
  %head
    %link{:rel => "stylesheet", :type => "text/css", :href => uri("/style.css")}
    %title= "Notable - #{@title || 'Notes'}"
  %body
    #container
      #doc
        %h1= @title || 'Notes'
        %ul
          - @notes.each do |note|
            %li= format_note(note)
